# Copy this to .env.local (for `vercel dev`) or set the variables in Vercel dashboard.

# Preferred: Use SendGrid (recommended)
SENDGRID_API_KEY=your_sendgrid_api_key_here

# Or fallback: SMTP settings (if not using SendGrid)
#SMTP_HOST=smtp.example.com
#SMTP_PORT=587
#SMTP_USER=smtp-user
#SMTP_PASS=smtp-pass

# Required: where to deliver messages
EMAIL_TO=you@yourdomain.com

# Optional: verified sender (SendGrid) or From address
EMAIL_FROM=website@yourdomain.com